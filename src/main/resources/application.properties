server.port=9003
server.address=127.0.0.1
logging.file.name=/opt/webapps/family-moments/logs/FamKidMem.log
server.error.whitelabel.enabled=false

spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

## default connection pool
spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5

## PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/famkidmem
spring.datasource.username=famkidmem
spring.datasource.password=famkidmem

#drop and create table again, good for testing, disabled, to prevent loss of data, enable via command line argument if development
#spring.jpa.hibernate.ddl-auto=create

server.error.path=/error
